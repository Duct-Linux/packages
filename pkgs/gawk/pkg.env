# gawk -- generated by tools/gen-recipes.sh, safe to edit.
# Source pins come from ../versions.env so a bump is one file.

SRC_URL=$GAWK_URL
SRC_SHA256=$GAWK_SHA256
SRC_DIR=$GAWK_SRCDIR

CONFIGURE_ARGS="--disable-nls"
