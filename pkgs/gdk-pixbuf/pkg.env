# gdk-pixbuf -- loads an image file into memory in a form GTK can draw.
#
# Not an LFS package, so it is pinned here rather than in versions.env.
SRC_URL=https://download.gnome.org/sources/gdk-pixbuf/2.42/gdk-pixbuf-2.42.12.tar.xz
SRC_SHA256=b9505b3445b9a7e48ced34760c3bcb73e966df3ac94c95a148cb669ab748e3c7
SRC_DIR=gdk-pixbuf-2.42.12

MESON_ARGS="-Dintrospection=enabled -Dman=false -Dtests=false -Dinstalled_tests=false -Dothers=enabled"
