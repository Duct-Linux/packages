[package]
name = "mpc"
description = "Complex number arithmetic library"
version = "1.3.1"
subversion = "2"
authors = ["upstream"]
packagers = ["ItzYanick"]

[source]
script = "../_scripts/fetch.sh"

[prepare]
script = "../_scripts/prepare.sh"

[build]
script = "../_scripts/build.sh"

[install]
script = "../_scripts/install.sh"

[dependencies]
glibc = ">=2.42.0"
gmp = ">=6.3.0"
mpfr = ">=4.2.2"

[dependencies.build]
