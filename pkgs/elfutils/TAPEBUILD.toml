[package]
name = "elfutils"
description = "libelf and libdw, the ELF and DWARF handling libraries"
version = "0.193.0"
subversion = "1"
authors = ["upstream"]
packagers = ["ItzYanick"]

[source]
script = "../_scripts/fetch.sh"

[prepare]
script = "../_scripts/prepare.sh"

[build]
script = "../_scripts/build.sh"

[install]
script = "install.sh"

[dependencies]
glibc = ">=2.42.0"
zlib = ">=1.3.1"

[dependencies.build]
