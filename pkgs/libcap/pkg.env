# libcap -- elogind, polkit and gdm all drop privileges through it rather than
# by being setuid, which matters here because tape cannot package a setuid bit
# at all.
#
# Not an LFS package, so it is pinned here rather than in versions.env.
SRC_URL=https://www.kernel.org/pub/linux/libs/security/linux-privs/libcap2/libcap-2.76.tar.xz
SRC_SHA256=629da4ab29900d0f7fcc36227073743119925fd711c99a1689bbf5c9b40c8e6f
SRC_DIR=libcap-2.76
