# python-mako -- a build-time dependency of mesa, which generates a large part
# of its GL dispatch tables and driver glue from Mako templates. mesa's meson
# checks for it and stops if it is absent.
#
# Not an LFS package, so it is pinned here rather than in versions.env.
SRC_URL=https://files.pythonhosted.org/packages/source/M/Mako/mako-1.3.10.tar.gz
SRC_SHA256=99579a6f39583fa7e5630a28c3c1f440e4e97a414b80372649c0ce338da2ea28
SRC_DIR=mako-1.3.10

PKG_ARCH=any
