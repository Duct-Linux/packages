# duct-filesystem has no upstream source; every file it ships is generated.
# pkg.env still has to exist because the shared helpers source it.

# The architecture this package is stamped with. "any" installs anywhere, which
# is right for a package of directories and text files.
PKG_ARCH=any
