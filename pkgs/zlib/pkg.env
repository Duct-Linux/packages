# zlib -- a hand-written configure, not autotools. It rejects --disable-nls and
# does not support building outside the source tree, so zlib carries its own
# build and install stages rather than using the generic ones.
SRC_URL=$ZLIB_URL
SRC_SHA256=$ZLIB_SHA256
SRC_DIR=$ZLIB_SRCDIR
