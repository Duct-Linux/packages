# meson -- the build system two thirds of the desktop stack has moved to.
#
# Not an LFS package, so it is pinned here rather than in versions.env.
SRC_URL=https://github.com/mesonbuild/meson/releases/download/1.8.2/meson-1.8.2.tar.gz
SRC_SHA256=c105816d8158c76b72adcb9ff60297719096da7d07f6b1f000fd8c013cd387af
SRC_DIR=meson-1.8.2

# Pure Python, so the same bytes install on every machine.
PKG_ARCH=any
