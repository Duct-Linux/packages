[package]
name = "libseccomp"
description = "High level interface to the kernel's seccomp syscall filter"
version = "2.6.0"
subversion = "1"
authors = ["Paul Moore and contributors"]
packagers = ["ItzYanick"]

[source]
script = "../_scripts/fetch.sh"

[prepare]
script = "../_scripts/prepare.sh"

[build]
script = "../_scripts/build.sh"

[install]
script = "../_scripts/install.sh"

[dependencies]
glibc = ">=2.42.0"

[dependencies.build]
pkgconf = ">=2.5.0"
gperf = ">=3.3.0"
