# gzip -- generated by tools/gen-recipes.sh, safe to edit.
# Source pins come from ../versions.env so a bump is one file.

SRC_URL=$GZIP_URL
SRC_SHA256=$GZIP_SHA256
SRC_DIR=$GZIP_SRCDIR

CONFIGURE_ARGS="--disable-nls"
