# glibc -- the C library everything else links against.
SRC_URL=$GLIBC_URL
SRC_SHA256=$GLIBC_SHA256
SRC_DIR=$GLIBC_SRCDIR

# The FHS patch is fetched by prepare.sh from the same pinned list.
PATCH_URL=$PATCH_GLIBC_FHS_URL
PATCH_SHA256=$PATCH_GLIBC_FHS_SHA256
